# Copy to .github/workflows/ci.yml in a Python project using uv.
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: mkdir -p /tmp/.uv-cache
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - run: uv sync --locked --all-extras --dev
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run mypy src/
      - run: uv run pytest --cov=src
      # WARNING: `|| true` skips failure on security issues.
      # Remove || true to enforce blocking on medium+ severity vulnerabilities.
      - run: uv run bandit -r src/ || true
      # WARNING: `|| true` skips failure on dependency vulnerabilities.
      # Remove || true to enforce blocking on unsafe packages.
      - run: uv run safety check || true
