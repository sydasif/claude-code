---
description: generate a self contained codebase graph
---

# Generate Codebase Graph

Analyze my codebase. Generate a self-contained HTML (Tailwind CDN, dark theme) with an interactive architecture diagram + an interactive Flows section (select and highlight the path + steps). And a JSON {nodes, edges, flows: [{steps}]} for AI agents. Deliver both complete.
