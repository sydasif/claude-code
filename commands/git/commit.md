---
description: Commit changes
---

1. **Inventory** — Show `git status`, `git diff`, `git diff --cached`, `git log --oneline -10`
2. **Commit code changes** — Stage all files with `git add`, then commit: `git commit -m '<type>(<scope>): <imperative summary>'`
